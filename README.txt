Reinforcement Learning Practical 2019/2020
Elevator Problem

Niels Rochell
Ivo E. Steegstra

(*) = recommended

Compile the program using:
(*) gcc -g -og -Wall -pedantic -std=c11 model.c -o model
or
gcc -g -og -Wall -pedantic -std=c11 model.c -o model -lm

Run using
./ model

Change parameters in model.c main(), instructions in comments

Parameter descriptions (insofar as they are not obvious) in dStructs.h

Uncomments code blocks in statistics.h printStats() and model.c run() for more output.
